 library(shiny)

# Run App -----------------------------------------------------------------
 # ui <- source("ui.R")
 # server <-source("server.R")
 # source("Global.R")
shinyApp(ui = ui, server = server)
